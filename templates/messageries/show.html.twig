{% extends 'base.html.twig' %}

{% block title %}Messageries{% endblock %}

{% block body %}
    <h1>Messageries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ messagery.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ messagery.message }}</td>
            </tr>
            <tr>
                <th>SentAt</th>
                <td>{{ messagery.sentAt ? messagery.sentAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_messageries_index') }}">back to list</a>

    <a href="{{ path('app_messageries_edit', {'id': messagery.id}) }}">edit</a>

    {{ include('messageries/_delete_form.html.twig') }}
{% endblock %}
